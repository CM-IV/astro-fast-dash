-- CreateTable
CREATE TABLE "Shortcut" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "thumbnail" TEXT NOT NULL,

    CONSTRAINT "Shortcut_pkey" PRIMARY KEY ("id")
);
